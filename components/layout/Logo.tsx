/* ============================================================================
 * The brand moved to Brand.tsx, where the emblem, wordmark and tagline are
 * one entity with a single `size` control.
 *
 * These aliases are kept so existing imports keep working. Prefer importing
 * Brand directly in new code.
 * ========================================================================= */
export { Brand as Logo, BrandMark as LogoMark, BrandLockup as LogoLockup } from './Brand'
